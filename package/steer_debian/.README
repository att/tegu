files in this directory that do NOT begin with a dot are copied to the bundle
driectory/debian and possibly modifed in that directory before building a
.deb package.  These files are considered a template. 

attlrqlite.debhelper.log
attlrqlite.dirs			list of directories that are targets or must exist on installed machine
attlrqlite.substvars
changelog				template file (format is very sensitive)
compat
control					describes the package (format is sensitive)
copyright
files
postinst				script executed after package is installed
postrm					script executed after package is removed (possibly on upgrade so be careful!)
rules


This might help:
https://www.debian.org/doc/manuals/maint-guide/index.en.html
